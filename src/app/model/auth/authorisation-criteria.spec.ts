import { AuthorisationCriteria } from './authorisation-criteria';

describe('AuthorisationCriteria', () => {
  it('it should create an instance', () => {
    expect(new AuthorisationCriteria()).toBeTruthy();
  });
});
