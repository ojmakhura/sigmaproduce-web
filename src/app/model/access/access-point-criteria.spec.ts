import { AccessPointCriteria } from './access-point-criteria';

describe('AccessPointCriteria', () => {
  it('it should create an instance', () => {
    expect(new AccessPointCriteria()).toBeTruthy();
  });
});
