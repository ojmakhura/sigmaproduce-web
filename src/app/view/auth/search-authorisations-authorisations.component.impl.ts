// Generated by andromda-jsf cartridge (view\table\table.component.impl.ts.vsl)
import { Component, Injector } from '@angular/core';
import { SearchAuthorisationsAuthorisationsComponent } from '@app/view/auth/search-authorisations-authorisations.component';

@Component({
  selector: 'app-search-authorisations-authorisations',
  templateUrl: './search-authorisations-authorisations.component.html',
  styleUrls: ['./search-authorisations-authorisations.component.scss'],
})
export class SearchAuthorisationsAuthorisationsImplComponent extends SearchAuthorisationsAuthorisationsComponent {
  override authorisationsColumns = ['id', 'accessPoint.name', 'accessPoint.url', 'actions'];

  constructor(private injector: Injector) {
    super(injector);
  }

  override doSearchAuthorisationsEdit(form: any): any {
    return form;
  }

  override ngAfterViewInit() {
    this.authorisations$.subscribe((authorisations) => {
      this.authorisationsDataSource.data = authorisations;
      this.totalElements = authorisations.length;
      this.authorisationsDataSource.paginator = this.authorisationsPaginator;
      this.authorisationsDataSource.sort = this.authorisationsSort;
    });
  }
}
